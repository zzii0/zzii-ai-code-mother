<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nylg.zziiaicodemother.mapper.UserMapper">
    <update id="updateUserAccountById">
        UPDATE user SET userAccount = #{userAccount} WHERE id = #{id}
    </update>

    <select id="selectDeletedByUserAccount" resultType="com.nylg.zziiaicodemother.model.entity.User">
        SELECT * FROM user WHERE userAccount = #{userAccount} AND isDelete = 1 limit 1
    </select>
</mapper>
